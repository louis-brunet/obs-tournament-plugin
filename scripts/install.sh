#!/usr/bin/env bash

# set -e 
#
# project_root="$(dirname "$(dirname "$(realpath "$0")")")"
# cd $project_root
#
# PLUGIN_NAME=obs-tournament-plugin
# OBS_PLUGINS_DIR=/usr/lib/x86_64-linux-gnu/obs-plugins
# OBS_DATA_DIR=/usr/share/obs/obs-plugins/
#
# mkdir -p "$OBS_PLUGINS_DIR"
# sudo cp "./build_x86_64/$PLUGIN_NAME.so" "$OBS_PLUGINS_DIR"
#
# mkdir -p "$OBS_DATA_DIR/$PLUGIN_NAME"
# sudo cp -r ./data/* "$OBS_DATA_DIR/$PLUGIN_NAME"
